spring:
  servlet:
    multipart:
      max-file-size: '100MB'
      max-request-size: '100MB'

app:
  cors:
    origins: ["*"]
  file:
    path-to-upload: "./uploaded"

logging:
  file:
    name: '${user.home}/logs/troubleshooter-editor.log'
